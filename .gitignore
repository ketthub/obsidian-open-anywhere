# User's personal config (each user creates their own from config.sh.example)
config.sh

# Windows: personal config (each user creates their own from config.example.ini)
config.ini

# Build artifacts
build/
*.app

# macOS metadata
.DS_Store
._*

# Logs
*.log

# Editor
.idea/
.vscode/
*.swp
